version: '3.1'
services:
  database:
    container_name: dbyoupi
    image: postgres
    environment:
      POSTGRES_USER : root
      POSTGRES_PASSWORD: toor
      POSTGRES_DB: dbase
    ports:
      - "5432:5432"
